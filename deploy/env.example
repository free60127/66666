# 回译本服务器环境变量模板 → 上传到服务器后改名为 /var/www/bts/.env 并填入真实值
# （值和 Render 控制台里配的 Environment 一致，迁移后云同步/账号数据无缝衔接）

PORT=8787

# Upstash KV（必填）：云同步、账号、批改任务持久化
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 账号找回邮件（可选，不填则邮件找回不可用，其余功能不受影响）
SMTP_USER=
SMTP_PASS=
